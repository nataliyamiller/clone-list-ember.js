<div class="col-md-7">
  <p>
  {{#each category.listings as |listing|}}
  {{listing-detail listing=listing}}
  {{/each}}
</p>
</div>

<div class="col-md-5">
<h4 class="add">Add new listing to this category</h4>
{{new-listing saveListing="saveListing" category=category}}
</div>
